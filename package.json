{
  "name": "picklinux",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "typecheck": "nuxt typecheck",
    "lint": "node scripts/lint.js",
    "test": "npx vitest run",
    "validate": "npm run validate:flow && npm run validate:questions && npm run validate:distros",
    "validate:flow": "node scripts/validate-flow.js",
    "validate:questions": "node scripts/validate-question-catalog.js",
    "validate:distros": "node scripts/validate-distros.js",
    "audit:distros": "node scripts/audit-distros.js"
  },
  "dependencies": {
    "nuxt": "^4.3.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@nuxtjs/i18n": "^10.2.3"
  }
}
